import { FilterRolesPipe } from './filter-roles.pipe';

describe('FilterRolesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRolesPipe();
    expect(pipe).toBeTruthy();
  });
});
