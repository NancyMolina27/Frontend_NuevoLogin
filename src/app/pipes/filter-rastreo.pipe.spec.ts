import { FilterRastreoPipe } from './filter-rastreo.pipe';

describe('FilterRastreoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRastreoPipe();
    expect(pipe).toBeTruthy();
  });
});
